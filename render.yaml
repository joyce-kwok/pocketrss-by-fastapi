services:
  # A Docker web service
  - type: web
    name: pocketrss-app
    runtime: python
    plan: free
    autoDeploy: false
    buildCommand: pip install -r requirements.txt
    startCommand: |
      uvicorn main:app --host 0.0.0.0 --port $PORT & gunicorn app:app --bind 0.0.0.0:$PORT
